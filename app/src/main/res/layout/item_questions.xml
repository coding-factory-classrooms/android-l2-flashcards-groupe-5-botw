<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootItem"
    android:layout_width="match_parent"
    android:layout_height="90dp">

    <ImageView
        android:id="@+id/questionImageView"
        android:layout_width="130dp"
        android:layout_height="75dp"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginTop="8dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/zelda" />

    <TextView
        android:id="@+id/difficultyTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:text="difficulty"
        app:layout_constraintBottom_toTopOf="@+id/previewQuestionTextView"
        app:layout_constraintStart_toEndOf="@+id/questionImageView"
        app:layout_constraintTop_toTopOf="@+id/questionImageView" />

    <TextView
        android:id="@+id/previewQuestionTextView"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:text="preview question"
        app:layout_constraintBottom_toBottomOf="@+id/questionImageView"
        app:layout_constraintStart_toStartOf="@+id/difficultyTextView"
        app:layout_constraintTop_toBottomOf="@+id/difficultyTextView" />
</androidx.constraintlayout.widget.ConstraintLayout>